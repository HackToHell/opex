{
  "name": "opex-e2e",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:api": "npx playwright test api",
    "test:grafana": "npx playwright test grafana"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
